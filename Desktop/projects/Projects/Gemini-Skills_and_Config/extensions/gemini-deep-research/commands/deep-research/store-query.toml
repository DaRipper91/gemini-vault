description = "Queries a file search store for grounded answers."
prompt = """
Query a file search store with the following question: {{args}}

1. Identify the appropriate store to query. If not specified, list stores using `file_search_list_stores` or infer from context (e.g., `.gemini-research.json`).
2. Call the `file_search_query` tool with the question and the store name.
"""